apiVersion: v1
kind: Service
metadata:
  name: {{ include "node-docker-app.fullname" . }}
  labels:
    {{- include "node-docker-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "node-docker-app.selectorLabels" . | nindent 4 }}
